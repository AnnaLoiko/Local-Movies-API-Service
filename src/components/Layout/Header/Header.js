import React, { useState } from "react";
import style from "./style.module";

import Logo from '@/components/Logo/Logo';

const Header = () => <header className={style.header}><Logo /></header>

export default Header;